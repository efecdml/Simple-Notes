<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SimpleNotes" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/secondaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primaryDarkColor</item>
        <item name="colorSecondaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@color/black</item>
        <item name="android:colorBackgroundFloating">@color/black</item>
        <!-- codes below didn't work.
        <item name="android:panelColorBackground">@color/black</item>
        <item name="android:colorButtonNormal">@color/black</item>
        <item name="android:colorBackgroundCacheHint">@color/black</item>
        <item name="android:actionBarItemBackground">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:actionBarPopupTheme">@color/black</item>
        <item name="android:actionBarWidgetTheme">@color/black</item>
        <item name="android:popupBackground">@color/black</item>

        <item name="actionModeBackground">@android:color/black</item>
        <item name="actionMenuTextColor">@android:color/white</item>

        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:background">@android:color/black</item>
        -->

        <!-- tried to remove margin between the items and the menu
            but didnt work
        <item name="android:listPreferredItemPaddingStart">0dp</item>
        <item name="itemVerticalPadding">0dp</item>
        <item name="android:itemPadding">0dp</item>
        <item name="itemSpacing">0dp</item>
        <item name="menuGravity">top</item>
        -->

        <!-- this darkens only item background not the menu
        <item name="android:itemBackground">@color/black</item>
        -->

    </style>
</resources>